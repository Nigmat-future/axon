"""Discovery scanners, organized by consent stage (see SECURITY.md).

Stage 1 (no prompt): environment variables — `env`.
Stage 2 (mention before reading): config files & .env — `config_files`.
Stage 3 (explicit opt-in): OS keychain — not yet implemented in M0.
"""
